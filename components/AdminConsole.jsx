"use client";

import { useEffect, useMemo, useState } from "react";

export default function AdminConsole() {
  const [tags, setTags] = useState([]);
  const [query, setQuery] = useState("");
  const [status, setStatus] = useState("all");
  const [selected, setSelected] = useState(null);
  const [error, setError] = useState("");

  useEffect(() => { fetch("/api/admin/tags").then(async (response) => { const data = await response.json(); if (!response.ok) throw new Error(data.error || "Admin access required."); setTags(data.tags || []); setSelected(data.tags?.[0] || null); }).catch((reason) => setError(reason.message)); }, []);
  const filtered = useMemo(() => tags.filter((tag) => (status === "all" || tag.status === status) && `${tag.code} ${tag.vehicle?.number || ""} ${tag.vehicle?.ownerPhone || ""}`.toLowerCase().includes(query.toLowerCase())), [tags, query, status]);
  const counts = useMemo(() => ({ total: tags.length, available: tags.filter((tag) => tag.status === "available").length, activated: tags.filter((tag) => tag.status === "activated").length }), [tags]);

  if (error) return <main className="admin-wrap"><section className="admin-error"><span className="section-kicker">Restricted workspace</span><h1>Admin access required</h1><p>{error}</p><a className="btn" href="/login">Go to login</a></section></main>;
  return <main className="admin-wrap"><header className="admin-header"><div><span className="section-kicker">Operations</span><h1>Tag inventory</h1><p>Track every printed QR tag from stock to an activated vehicle.</p></div><span className="admin-badge">Admin console</span></header><section className="admin-stats"><div><span>Total tags</span><strong>{counts.total}</strong></div><div><span>Available to ship</span><strong>{counts.available}</strong></div><div><span>Activated</span><strong>{counts.activated}</strong></div></section><section className="admin-layout"><div className="admin-table-panel"><div className="admin-toolbar"><input aria-label="Search tag inventory" onChange={(event) => setQuery(event.target.value)} placeholder="Search ID, vehicle, or owner" value={query} /><div className="admin-filters">{["all", "available", "activated", "disabled"].map((item) => <button className={status === item ? "active" : ""} key={item} onClick={() => setStatus(item)} type="button">{item}</button>)}</div></div><div className="admin-table"><div className="admin-table-head"><span>Tag ID</span><span>Status</span><span>Vehicle</span><span>Owner</span></div>{filtered.map((tag) => <button className={`admin-row ${selected?.id === tag.id ? "selected" : ""}`} key={tag.id} onClick={() => setSelected(tag)} type="button"><strong>{tag.code}</strong><span className={`tag-status ${tag.status}`}>{tag.status}</span><span>{tag.vehicle?.number || "Unassigned"}</span><span>{tag.vehicle?.ownerPhone || "—"}</span></button>)}{filtered.length === 0 ? <p className="admin-empty">No tags match this view.</p> : null}</div></div>{selected ? <aside className="admin-detail"><div className="detail-top"><span className={`tag-status ${selected.status}`}>{selected.status}</span><span className="section-kicker">Tag details</span></div><div className="print-tag"><div className="print-tag-brand"><span className="brand-mark">P</span><strong>PingTag</strong><span>PRIVACY-FIRST PARKING</span></div><div className="print-tag-copy"><span className="print-tag-kicker">Scan to reach the owner</span><h2>{selected.vehicle?.number || "Your vehicle"}</h2><p>Send an anonymous alert without exposing your phone number.</p></div><img className="admin-qr" alt={`QR code for ${selected.code}`} src={selected.qrDataUrl} /><div className="print-tag-footer"><strong>Scan. Send. Stay private.</strong><span>{selected.code}</span></div></div><div className="admin-detail-actions"><button className="btn secondary" onClick={() => window.print()} type="button">Print premium tag</button><a className="admin-scan-link" href={selected.scanUrl} target="_blank" rel="noreferrer">Open scanner ↗</a></div>{selected.vehicle ? <dl><div><dt>Vehicle</dt><dd>{selected.vehicle.number}</dd></div><div><dt>Owner account</dt><dd>{selected.vehicle.ownerPhone || "Not available"}</dd></div><div><dt>Private phone</dt><dd>{selected.vehicle.phoneNumber}</dd></div><div><dt>Location</dt><dd>{[selected.vehicle.societyName, selected.vehicle.flatNumber].filter(Boolean).join(" · ") || "Not provided"}</dd></div></dl> : <div className="detail-empty"><strong>Ready for dispatch</strong><span>This tag has not been activated by a customer.</span></div>}</aside> : null}</section></main>;
}
